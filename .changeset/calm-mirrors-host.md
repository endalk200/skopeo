---
"@skopeo/api": minor
"@skopeo/web": minor
---

Publish the Platform API and Web Application as the first shared Platform Release, with production OCI images and a version-matched self-hosting bundle. API documentation moves to `/api/docs` and `/api/openapi.json`, and OTLP export becomes optional while console logging remains enabled.
