# Shared packages

These packages contain behavior used across runtime contexts. They may depend on other shared packages and repository tooling, but must not depend on CLI-, API-, or web-owned packages.
