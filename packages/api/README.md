# API packages

This group is reserved for internal packages owned by the hosted API runtime. The current Projects module remains in `apps/api` because it has one consumer and does not yet earn an independent package seam.
