# Web packages

This group is reserved for internal packages owned by the web runtime. Web modules remain in `apps/web` until an independent package gives them concrete reuse, testing, or ownership leverage.
