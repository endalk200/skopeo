# @skopeo/cli

Skopeo is a code review agent for analyzing code changes and reporting review findings through local and hosted developer workflows.

```sh
npm install -g @skopeo/cli
skopeo --help
```
