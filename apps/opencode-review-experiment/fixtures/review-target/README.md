# Review Target Fixture

This is the default Review Target for the OpenCode SDK experiment. It is intentionally tiny so `inspect` and `review --no-reply` can exercise the SDK without pointing at the whole repository.

Use `--target /path/to/repo` to run the same experiment against another working tree.
