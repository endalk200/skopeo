Review this Review Target:

Target: `{{target}}`

Current git status:

```text
{{git_status}}
```

Current diff summary:

```text
{{git_diff_stat}}
```

Focus on the current working-tree state and produce a concise Review Report. Use tools as needed to inspect the target.
