/**
 * Marketing feature — the public-facing landing page. The platform will
 * live in a sibling feature module; keeping marketing self-contained here
 * makes that separation clean.
 */
export { Landing } from "./components/landing";
