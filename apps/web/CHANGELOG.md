# @skopeo/web

## 0.2.0

### Minor Changes

- 0727a4a: Publish the Platform API and Web Application as the first shared Platform Release, with production OCI images and a version-matched self-hosting bundle. API documentation moves to `/api/docs` and `/api/openapi.json`, and OTLP export becomes optional while console logging remains enabled.
